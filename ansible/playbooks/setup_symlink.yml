- name: Setup Symbolic Links for Application Data
  hosts: host, workers
  become: yes
  tasks:
    - name: Create symbolic link for results directory
      file:
        src: /mnt/results
        dest: /var/www/html/results
        state: link
