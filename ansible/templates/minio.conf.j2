server {
  listen 80;
  server_name {{ minio_ingress_hostname }};
  
  location / {
    proxy_pass http://{{ minio_ingress_hostname }}:9000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
