{
  "version": "10",
  "aliases": {
    "local": {
      // Use https or http depending on your TLS setup
      "url": "https://{{ ansible_default_ipv4.address }}:9000",
      "accessKey": "myminioadmin",
      "secretKey": "{{ lookup('ansible.builtin.file','./.miniopass') }}",
      "api": "s3v4",
      "path": "auto"
    }
  }
}
