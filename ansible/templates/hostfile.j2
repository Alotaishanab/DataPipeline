# Worker nodes
{% for node in groups['worker_inventory'] %}
{{ hostvars[node].ansible_default_ipv4.address }} workernode{{ loop.index }}
{% endfor %}

# Management node (from delegated fact)
{{ hostvars['localhost']['mgmt_ip'] }} mgmtnode

# Localhost fallback
127.0.0.1 localhost
